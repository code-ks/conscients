<nav aria-label="breadcrumb">
  <ol class="breadcrumb">
    <% category.path.each do |c| %>
      <li class="breadcrumb-item <%= "active" if c == category %>">
        <%= link_to c.name, category_products_path(c) %>
      </li>
    <% end %>
  </ol>
</nav>
