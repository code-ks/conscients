<div class="container-conscients">
  <% if @products.any? %>
    <%= render 'breadcrumb', category: @category %>

<div>
<h1><%= @category.name %></h1>
<p>Texte de presentation de la catégorie <%= @category.name%> sur deux ou trois lignes maximum, il faut donc creer un category.title et un category.description</p>
</div>

    <div class="d-flex">
      <%= render 'filters', category: @category %>
    </div>
    <div class="card-deck">
      <%= render @products %>
    </div>
    <div class="d-flex justify-content-center"><%= paginate @products %></div>
  <% else %>
    <h2><%= t('.no_product') %></h2>
  <% end %>
</div>
