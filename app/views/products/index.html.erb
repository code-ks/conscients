<% set_meta_tags title: I18n.t('meta_tags.products.index.title'),
                 description: I18n.t('meta_tags.products.index.description'),
                 keywords: I18n.t('meta_tags.products.index.keywords') %>

<div class="container-conscients">
  <% if @products.any? %>
    <%= render 'breadcrumb', category: @current_category %>

  <div class="container-conscients page-font">
    <h1><%= @current_category.name%></h1>
  </div>

    <div class="carousel-container summary">
      <p><%= @current_category.description%></p>
    </div>
    <div class="side-line-filter"></div>
<div class="summary">
    <div class="d-flex align-items-center filters-margin side-line-filter-hide">
      <div class="top-menu-font title-grey mb-0 pr-5">Filtrer par :</div>
      <%= render 'filters', category: @current_category %>
    </div>
</div>


    <div class="card-deck">
      <%= render @products %>
    </div>
    <div class="d-flex justify-content-center"><%= paginate @products %></div>
  <% else %>
    <h2><%= t('.no_product') %></h2>
  <% end %>
</div>

